We now have a functional host. However, it can't do a whole lot without an :py:class:`Action`
to exercise the host and its environments. If we were to run this host, we would
get some lackluster output: